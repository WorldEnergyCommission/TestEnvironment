# lynus-dpc-rf

dpc algorithm for room temperature control, made for Lynus integration


## Local development setup

More or less the same as in the root folder

1. copy `.env.local.example` as `.env`
2. add all variables which are missing
3. run `docker compose up -d --build`